const mongoose = require("mongoose")

const messageCountScehma = mongoose.Schema({
    // the user ID
    _id: {
        type: String,
        required: true
    },

    messageCount: {
        type: Number,
        required: true
    }
})

module.exports = mongoose.model('message-counts' , messageCountScehma)